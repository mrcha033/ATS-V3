#include "exchange_interface.hpp"
#include "../utils/logger.hpp"

namespace ats {

// Since ExchangeInterface is a pure virtual interface,
// this file exists to satisfy the build system but contains no implementations.
// Concrete exchange implementations will derive from this interface.

} // namespace ats 